<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property name="APP_NAME" value="cuscrud-backend"/>
    <property name="LOG_DIR" value="${LOG_DIR:-logs}"/>
    <property name="GENERAL_LOG_FILE" value="${LOG_DIR}/${APP_NAME}.log"/>
    <property name="APPLICATION_LOG_FILE" value="${LOG_DIR}/${APP_NAME}-application.log"/>
    <springProperty scope="context" name="APP_LOG_LEVEL" source="logging.level.br.com.cuscrudrest" defaultValue="INFO"/>
    <springProperty scope="context" name="ROOT_LOG_LEVEL" source="logging.level.root" defaultValue="WARN"/>
    <property name="LOG_PATTERN"
              value="%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX, America/Recife} %-5level [%thread] %logger{36} request_id=%X{request_id:-system} client_ip=%X{client_ip:-system} %msg%n"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <appender name="GENERAL_FILE" class="ch.qos.logback.core.FileAppender">
        <file>${GENERAL_LOG_FILE}</file>
        <append>true</append>
        <immediateFlush>true</immediateFlush>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <appender name="APPLICATION_FILE" class="ch.qos.logback.core.FileAppender">
        <file>${APPLICATION_LOG_FILE}</file>
        <append>true</append>
        <immediateFlush>true</immediateFlush>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <logger name="br.com.cuscrudrest" level="${APP_LOG_LEVEL}" additivity="true">
        <appender-ref ref="APPLICATION_FILE"/>
    </logger>

    <root level="${ROOT_LOG_LEVEL}">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="GENERAL_FILE"/>
    </root>
</configuration>
